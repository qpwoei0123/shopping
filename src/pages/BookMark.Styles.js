import styled from "styled-components";

export const S = {
    ImageContainer: styled.div`
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;
    `
}